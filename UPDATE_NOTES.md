# ポートフォリオサイト 更新メモ

対象ファイル：`C:\Users\kurod\projects\portfolio-site\index.html`
公開URL：https://kuroda-daisuke-portfolio.vercel.app/
GitHub：https://github.com/daisuke7330/portfolio.git

---

## 2026-08-19 ｜ 4件目の実績「既存サイトのWordPress化」を追加

### 何をしたか

**Works（3業種のサイト）セクションの直後に、独立した新セクションを1つ追加しました。**
既存の3案件（LA REINE / 結-YUI- / Nexus）は**1文字も触れていません。**

追加したのは以下の3か所だけです。

| # | 場所 | 内容 |
|---|---|---|
| 1 | ヘッダーのナビ | 「不動産」と「プロフィール」の間に `WordPress化`（`#work-wp`）を追加 |
| 2 | `<style>` 内 | `.sec-head h2 em{font-style:normal;color:var(--accent);}` を1行追加（見出しの「9時間」をアクセント色にするため。既存のh2にemは1つも無いので影響なし） |
| 3 | Works セクションと Profile セクションの間 | 新セクション `CMS Migration` を丸ごと追加（`<article class="work" id="work-wp">` を含む） |

### なぜ「独立した新セクション」にしたか

既存 Works セクションの見出しは **「3業種のサイトを、一人で。」**。
ここに4件目を足すと**この見出しを書き換える必要が出て、既存の記述を壊すことになります。**
また今回の実績は「新規制作」ではなく「既存サイトのCMS移行」で、性質が違います。

そこで**別セクションとして独立**させ、見出しで時間を前面に押す形にしました。
デザインは既存の `.work` `.shot` `.points` `.chips` `.visit` をそのまま使っているため、
見た目は既存3案件のカードと完全に揃っています。

### 打ち出しの軸（本人指示どおり「時間」を押す）

- セクション見出し：**「公開中のサイトを、9時間でWordPressに。」**
- カード左の大きい数字：**`9 時間`**（既存3案件の「62ページ」「24ページ」と同じ位置・同じ書式）
- その下：`静的HTML 全23ページ を移行`

### 記載した事実（これ以外は書いていません）

- 自作の静的サイト「Nexus」**全23ページ**を、WordPressの**オリジナルテーマを自作**して移行
- **実稼働 約9時間**（→ 下の【要確認】参照）
- **ACF等のプラグインを一切不使用**。カスタム投稿タイプ4種／カスタムタクソノミー2種／
  カスタムフィールド17項目を、すべて素のメタボックスで実装
- テンプレート15枚＋パーツ2枚／CSS 7本／JS 2本
- 投稿20件・画像30点を **WP-CLIのスクリプトで一括投入**
- 元の静的HTMLとWordPress版を突き合わせる**検証ツールを自作**し、**全15ページで一致**を確認
- AIチャットボットは REST API のカスタムエンドポイントで移設

---

## 【2案あります】リンクをどうするか — 選んでください

WordPress版はまだローカル（http://nexus.local）のみで、**公開URLがありません。**
そこで2案作りました。**現在は 案A を入れてあります。**

### 案A（現在採用中）— 移行元の静的サイトにリンクする

- カード画像とボタンのリンク先： `https://nexus-hp.vercel.app/index.html`
- ボタンの文言：**「移行元のサイトを見る」**
- カード上の注記：
  > ※ 自主制作サイト「Nexus」を題材にした移行実績です。「見た目を1ピクセルも変えない」ことを
  > 条件にしたため、画像は移行元と同じものを掲載しています。WordPress版はサーバーへの公開を
  > 準備中のため、リンク先は移行元の静的サイトです。

**メリット：** クリック先がある（離脱しない）。「見た目を変えていない」ことの証明にもなる。
**デメリット：** 飛んだ先は静的サイトなので、WordPress版そのものは見せられない。

### 案B — リンクを置かない

公開URLが出るまでボタンを出さない形です。案Bにする場合は、`index.html` の
`id="work-wp"` のブロック内で、以下の**2か所**を書き換えます。

**① 画像のリンクを外す（1行目）**

置き換え前：
```html
      <a class="shot si-nexus" href="https://nexus-hp.vercel.app/index.html" target="_blank" rel="noopener" aria-label="WordPress化の移行元となった不動産アセットマネジメント Nexus のファーストビュー"></a>
```
置き換え後：
```html
      <div class="shot si-nexus" role="img" aria-label="WordPress化の移行元となった不動産アセットマネジメント Nexus のファーストビュー"></div>
```

**② ボタンを消す**

以下の1行を削除します。
```html
        <a class="visit" href="https://nexus-hp.vercel.app/index.html" target="_blank" rel="noopener">移行元のサイトを見る</a>
```

**メリット：** 誤解の余地がゼロ。
**デメリット：** カードにクリック先が無くなる。

> **おすすめは案A。** WordPress版をサーバーに公開したら、案Aのリンク先だけを
> 新しいURLに差し替え、ボタンの文言を「WordPress版のサイトを見る」に変えるのが一番きれいです。

---

## 【要確認】

1. **「約9時間」の根拠**
   `nexus-hp` リポジトリの git コミット履歴の作業時間帯から算出した概算です。
   - 2026-08-18 00:44 〜 03:19（約2.5時間）
   - 2026-08-18 23:46 〜 2026-08-19 03:17（約3.5時間）
   - 2026-08-19 08:18 〜 08:19、13:51 のコミット分
   → **合計おおむね9時間。本人の体感と合っているか確認してください。**
   合わない場合はサイトの2か所（見出しの `<em>9時間</em>` と `.fig-big` の `9 時間`）を直します。

2. **ページ数を「23」で書いています**
   静的サイトのHTMLファイルは24本ありますが、うち `nexus-asset.html` はどこからも
   リンクされていない孤立ページです。`PORTFOLIO.md` の方針に合わせて **23** で数えました。
   一方、**既存のNexusカードは「24ページ」のまま**にしてあります（既存の記述は触らない指示のため）。
   → **同じサイトが 24ページ と 23ページ で並ぶことになります。どちらかに揃えるか判断が必要です。**

3. **合計ページ数の表記**
   サイト内に「全110ページ」などの合計値は**使われていませんでした**（確認済み）。
   したがって合計の書き換えは不要です。
   ただし `PORTFOLIO.md` の統合ポートフォリオ欄には「全110ページ／実稼働 合計60時間」があります。
   今回は**新規制作ではなく移行**なので、**ページ数は足していません。**
   足す／足さないの最終判断をお願いします。

4. **ヒーローの横スライドは3枚のままです**
   スライドの `aria-label` が「制作したサイト（3案件）」で、WordPress版は公開URLが無く
   見た目も移行元と同一のため、4枚目は追加していません。
   公開URLが出たら4枚目を足す価値があります。

5. **既存の「月3〜4商品の広告企画を主導」という記述**
   コンセプト欄（`.why-credential`）に元から入っています。
   グローバルのルールでは**この書き方は避ける**ことになっています
   （担当範囲を過少に見せ、「総売上の約45%」と整合しないため）。
   今回は**既存部分に手を入れない指示のため、そのままにしてあります。**
   直す場合は「新聞全体を担当し、その中で月3〜4件の新商品を導入している」という構造に変えます。

---

## 品質チェックの結果（2026-08-19 実施）

| 項目 | 結果 |
|---|---|
| 既存3案件の記述 | **無傷**。編集前後の差分は**追加3か所のみ**で、削除・変更はゼロ |
| HTMLタグの対応 | **OK**。スタックで検証し、未閉じタグ0・不整合0 |
| 内部アンカー | **OK**。`#top #concept #work-reine #work-yui #work-nexus #work-wp #profile #contact` すべて対応するidが存在 |
| 外部リンク | **全4件 200**（reine-clinic / patisserie-yui / nexus-hp / クラウドワークス） |
| 画像 | すべて base64 のインライン埋め込み。外部参照ゼロ＝リンク切れの余地なし |
| レスポンシブ（1280px） | 横スクロールなし（文書幅1265px ≦ 1280px） |
| レスポンシブ（375px） | 横スクロールなし。`.work-main` が1列に、`.side` が横並びに切り替わることを実測で確認 |
